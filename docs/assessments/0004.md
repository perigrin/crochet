---
stability: 1
covers:
  - docs/decisions/0004-architecture-synthesis.md
---

# Assessment: 0004, the architecture document is a synthesis of accepted decisions

Threshing session over `docs/decisions/0004-architecture-synthesis.md`, round
one, at `6a18943`. Outcome: **another round owed**. Both participants recommend
**modify**, each with blocking findings outstanding.

The file is named after the decision because no milestone exists yet.
`crochet:refinement` renames it when it creates one.

## Participants

| | Role | Context |
|---|---|---|
| `codebase-lens` | participant | dispatched fresh; the spec against the code and the decisions in force |
| `spec-lens` | participant | dispatched fresh; `paad:pushback`'s taxonomy, run non-interactively |
| this session | drafter | holds no view on the subject; plausibly the author, so it sits as drafter rather than participant |

Both reported. Neither round ran short.

**`spec-lens` ran its lens without the one-issue-at-a-time resolution loop**,
because a dispatched participant has no user to resolve issues with. It ran
Phase 1, Phase 1.5 and Phase 2 and returned every finding at once. That is a
weaker administration of the lens than the skill describes and the record says
so rather than implying otherwise.

## The three axes

**Codebase.** The document's factual claims about this repository largely hold,
and both participants checked them independently rather than taking them.

- The ten-row gap table is correct on nine rows. "The decision series" is
  contestable: `plugin-structure.md` names `docs/decisions/` and cites 0003 by
  number and title. What is absent is the series *as a concept* — numbering,
  `state:`, supersession — not the directory.
- Both `xt/run.sh` claims are accurate. `xt/run.sh:73` is literally
  `for d in "$ROOT"/docs/architecture "$ROOT"/docs/contributing`.
- The absorption claim holds section by section. The only content with no home
  is the "It previously said the pipeline runs in strict order" paragraph, which
  is temporal narration a live document already forbids.
- The git-zhi behavioural claim was re-observed on **0.7.2**, not taken from the
  document's "Observed on 0.6.0". Both participants built scratch repositories
  and agree: `docs/ARCHITECTURE.md` appears in `docs health --format json` and
  its dead links in `docs check`; a root-level `ARCHITECTURE.md` appears in
  neither. The marker should be updated to the version it was re-observed on.

**Architecture.** This is where the findings are. Three of them are collisions
with decisions in force rather than defects in argument, and they are listed
below.

**Direction.** Both participants say yes, and `codebase-lens` says the document
understates its own strongest argument. `CLAUDE.md` imports the architecture
document into every agent invocation, and what that import currently delivers is
a directory tree and three tables. Ten decided architectural rules reach no agent
unless it goes reading the archive — the "dynamic lookup that may not happen"
`CLAUDE.md` itself warns about. Moving them into the imported document is a move
down 0001's own enforcement ladder, from rung 4 to rung 3, for the whole ruleset
at once.

## What blocks

Both participants raised the first two independently, from different briefs,
without knowledge of each other.

**1. The citation check contradicts the section plan at landing.** The accepted
decisions are 0001, 0002 and 0003. The Sections table cites `[0001]` and
`[0002]` only; 0003 appears in prose about `amends` and in References, never as
a section source. So on landing either `sh xt/run.sh` goes red — which is AC8 —
or the synthesis gains a citation of 0003 that exists to satisfy the check
rather than carry a claim, the exact failure 0004 names in its own Completeness
paragraph. And 0003's substance cannot easily go there: the text 0004 absorbs
verbatim explicitly refuses to carry it, "policy rather than architecture...
This document carries the pipeline's shape and stops there". The same collision
recurs when 0004 itself flips to `accepted`, since nothing cites 0004 either.

**2. Acceptance criterion 6 is already green on an untouched tree.**

```
$ sh xt/run.sh xt/fixture 2>&1 | grep -q 'cites' && echo GREEN
GREEN
```

The matching line is the *existing* cite-symbols check: `FAIL:
docs/decisions/0004-citation.md cites a line number, not a symbol:
graph.go:439`. The criterion passes whether or not the check it exists to prove
is ever written. The repository's own defect class, inside the verification of
the decision that adds checks against it. The other seven criteria are red and
none is unrunnable. One-line fix: a distinguishing substring that does not
contain `cites a line number`.

**3. The criteria set is the count the document rejects.** A
`docs/ARCHITECTURE.md` whose entire content is `[0001] [0002] [0003]` satisfies
all eight acceptance criteria. That is the straw man 0004 constructs and
dismisses — "a document listing every decision's headings at the bottom would
satisfy any such count while reflecting none of them" — reproduced in the
document's own verification. Seven criteria check placement and wiring; the
deliverable is checked by `test -f`. Since `git-zhi-verify` runs these at
`milestone edit --state complete`, the milestone can close green on a stub.

**4. Doc-first has no trigger for an accepted-but-unbuilt decision.** The check
fires on `state: accepted`. Doc-first obliges only "a decision's implementing
pull request", which an unbuilt decision does not have. So an accepted-but-unbuilt
decision must appear as a present-tense claim in the document `CLAUDE.md:17`
imports into every agent invocation — which is the failure 0001 exists to
prevent, and 0001 names accepted-but-not-built as its contribution over ADR and
RFD. 0004 provides no form for this case. 0005 and 0007 are already placeholders.

**5. The trigger nominated to schedule assess cannot fire.** 0004 says "`git zhi
docs health` reporting drift on the document is the trigger to run assess over
it." Drift is computed from the document's **mtime**. Measured on this worktree:

```
Summary: 9 docs checked, all current
docs/architecture/plugin-structure.md   mtime 2026-09-21T14:25:57
docs/assessments/rfc-0003.md            mtime 2026-09-21T14:25:57
docs/assessments/walkthrough-assess.md  mtime 2026-09-21T14:25:57
```

Every mtime is the checkout timestamp, identical across all nine. In any fresh
clone or worktree — which is where CI runs and where every dispatched agent
works — churn is 0 and drift is NONE for every document, permanently. So
"assess carries correctness" degrades to "someone remembers".

`development-workflow.md` already records two ways this tool reports all-clear
over nothing. This is a third, and it is a defect in git-zhi rather than in
0004; it belongs in `docs/requests/` whatever 0004 does.

The same mechanism is mistuned in the other direction too: if the trigger did
fire it would fire on ordinary `skills/` churn, and `assess.md` Step 0
mandatorily delegates to `crochet:discernment` — so each firing summons a
multi-agent session. 0004's one Open Question is the one that would tune it, and
it says "Neither has been run."

## What else was raised

**Collisions with decisions in force.**

- **0001's second acceptance criterion goes permanently red.**
  `0001:566` is `test -n "$(ls docs/architecture/*.md 2>/dev/null)"`. 0004
  removes that directory and asserts `! test -e docs/architecture` as its own
  AC3. Nothing detects it: `xt/run.sh` does not execute decision criteria. This
  is the shape `coding-conventions.md` already records biting 0002.
- **"Two rules change; the rest holds" understates the blast radius.**
  `docs/architecture` appears in 0001 at lines 97, 112, 157, 387, 479, 502 and
  566. Six are archive prose, fine to leave stale under 0001's own as-of-date
  rule — but a reader is told two rules moved and finds seven sites with no way
  to sort deliberate staleness from oversight. One sentence closes it.
- **0004 removes the damping 0001 named as its mechanism.** 0001: "stingy with
  live, generous with archive... almost nothing is promoted out of it, and that
  asymmetry, not a threshold, is what damps the feedback loop." 0004 institutes
  obligatory promotion. That may be the right trade; it is a third changed rule
  the amendment section does not declare, and it should be argued rather than
  left for a future reader to find as a contradiction between two documents both
  in force.

**The runnable-criteria question, which is not 0004's to settle.** `spec-lens`
blocks on 0004 carrying eight runnable criteria at all, citing
`coding-conventions.md`'s "A decision does not carry runnable acceptance
criteria" (`d89e288`, 2026-09-20). A third session reviewing the merged branch
argued the reverse from the same evidence: that the sentence contradicts
accepted 0001, which *requires* runnable criteria — "one runnable command per
checkbox line in a paren-wrapped backtick span" — and that 0003 does not settle
the division the sentence cites it for.

The evidence that the broad rule is not the operative one is that nothing
follows it: at `6a18943`, 0001 carries 8 runnable criteria, 0003 carries 33 and
0004 carries 8. Only 0002 was stripped, and 0002 was the one whose criteria had
gone permanently red.

**This gates 0004 and is not a finding 0004 can answer**, because 0004 is the
decision that amends 0001. It was recorded here and referred out.

**Resolved between rounds, in `19948bf` and `2e0bb6f`.** A decision carries
runnable criteria, in the form 0001 requires; the two narrow constraints
underneath survive; and the citation to 0003 is dropped, because 0003 still
carries thirty-three runnable criteria of its own and cannot be the source of a
rule forbidding them. `spec-lens` supplied the load-bearing reason when asked
for a view: the defect was less the rule than its installation, since a
contributing document cannot override an accepted decision by prose one import
away — rung 4 pre-empting rung 1 of 0001's own ladder. `spec-lens`'s finding [1]
falls with it. It is the only round-one finding that does.

**Checks that fail open.** Both proposed checks iterate what the synthesis
cites, so zero citations yields zero findings — indistinguishable from a perfect
document. The runner's three existing per-file loops open `[ -f "$f" ] ||
continue`, and `xt/run.sh` carries a comment recording that exact bite. Both
checks must read `$ROOT/docs/ARCHITECTURE.md`; the runner carries two comments
about that trap and 0004 never says `$ROOT`.

**The check is weaker than the document says.** 0004 calls it weak, "catches a
decision cited nowhere and nothing finer". Because the citation form is `[NNNN]`
*linked to the file*, the real predicate is "the four-digit string appears in
the file" — satisfied by a link, a code fence, a References entry, or a sentence
reading "0003 is deliberately not synthesised here". The document's written
description of its own weakness is what it offers a reader in place of a
stronger check, so understating it matters.

**`xt/fixture/expected` is not in scope and it is what keeps the new checks
honest.** It declares `# count: 9`, has that count checked against its entries,
carries a "9 of 26 sites" ledger saying "Recount when a site is added", and is
the only thing that detects a check going quiet. Two fixture cases landing
without two `expected` entries means the self-test passes while proving nothing
about the new checks.

**Smaller.** Retargeting the CONTRIBUTING link falsifies the sentence two lines
below it ("Every link here points at a directory that holds a file"), and Scope
of Change does not mention it. Three by-path citations to
`plugin-structure.md` go dangling — twice in `0003`, once in `0006` — two of
them inside an accepted decision, in the class `coding-conventions.md` states it
cannot detect. 0004 says "0003 is `proposed`" and builds an argument on it; 0003
is accepted, so the paragraph is moot rather than wrong. The table row
"compiler, not runtime" names a concept no decision decides — `codebase-lens`,
and **wrong**: see round three. AC2 shells out to `python3` where a `grep` does
the job in the repository's own language — `codebase-lens`'s finding;
`spec-lens` filed the same observation as one of its seven discards, for want of
a consequence. The always-imported context grows with the series, compelled by a
check rather than by editorial judgment.

**And one finding's rationale was lost between the two documents.** 0004's
absorption section once explained *why* the skill-adding procedure is not named
as something that "does not move": `plugin-structure.md` has never held such a
section, so an implementer following the absorption would go looking for one.
The decision trimmed the clause and this file did not pick it up, which
`codebase-lens` caught in round four. It is recorded here because that is where
it belongs, and because a trim that drops a rationale on the floor is the
failure the trim was meant to prevent.

## Standing aside

Recorded with grounds, not blocking, as each participant declared it.

- **`codebase-lens`, on a rung-2 completeness check.** "A decision landed
  without touching the synthesis" *is* mechanically checkable — for each
  decision whose Scope of Change names `docs/ARCHITECTURE.md`, does a commit
  carrying its `Implements:` trailer touch that file? `xt/run.sh` already reads
  those trailers. 0004 conflates that with "is the synthesis *good*", and only
  the second is uncheckable. It believes this is right and buildable; it accepts
  that the decision's judgment is defensible and does not hold on it.
- **`spec-lens`, on unbounded growth of the always-imported document.** It turns
  on how long the synthesis actually is, which the author knows and the
  participant does not. If the intent is a short claims-with-citations document,
  the concern dissolves.

## Discards

`codebase-lens` dropped 3 for want of a mechanism; `spec-lens` dropped 7. Named
in both cases rather than left implicit, since a review that reports findings
and says nothing about its discards looks like it stopped early.

## Round one's outcome

**Another round owed.** Both participants recommend modify and neither released
a finding. Nothing was changed in the tree during the round: the subject held
still for its duration.

The runnable-criteria question above was referred out of the session, because it
is a question about what 0001 requires and 0004 is the decision that amends
0001. It is not answerable from inside an assessment of 0004. It has since been
answered, and the answer is recorded with the finding rather than here.

Round two opens against a different tree: this branch now carries the four
corrections that the same review found in `pu`, so a participant resumed here
reads the conventions text that is in force rather than the one round one read.
That is a change to the subject's surroundings between rounds, not to the
subject, and the participants are told which.

# Round two — `47497f6`

Both participants recommend **modify**. `codebase-lens` released ten of twelve
and raised eight new; `spec-lens` released nine, partly released one, and raised
seven. **Another round owed.**

**Round one found defects in the document. Round two found defects in the
fixes**, and four of `codebase-lens`'s eight new findings were inside the single
change the author was most confident about — keying the citation check to
`Implements:` trailers.

## What the fixes broke

**The criteria could not go green.** Criterion 7 — `! grep -rq
'architecture/plugin-structure.md' docs/decisions/` — matched its own line.
Criteria 8 and 10 required the fixture's synthesis to be simultaneously present
and missing. Criterion 9's population came from `git log` run in a directory
that is not a repository, so it resolved outward and read crochet's own
trailers: both participants verified this independently and saw `Implements`
trailers `0001 0002 0003`.

That last is the session's defect class reappearing inside its own remedy — a
check reporting on a subject that is not the one named, introduced by the fix
for a check reporting on a subject that was not the one named.

`spec-lens` stated the trade the revision made:

> Round one's criteria set had one criterion green before any work — a false
> pass that blocked nothing. Round two's set has three that cannot go green at
> all, which block `milestone edit --state complete` outright. Same defect
> family, more expensive failure.

**A correct row was cut from the gap table on a false ground.** The revision
removed "compiler, not runtime", stating it appears in
`docs/contributing/development-workflow.md` and nowhere in the series. It is a
section heading in `0001-documentation-architecture.md` and appears nowhere
under `docs/contributing/`. `codebase-lens` called it the one place the revision
was worse than what it replaced. It originated in `spec-lens`'s round-one
finding [10], which was itself wrong — recorded under round three.

**Nothing in the loop read the synthesis.** The revision answered "what judges
it" with `crochet:review`. `grep -n -i 'live doc\|architecture\|synthesis\|doc-first'
skills/review/review.md` returns nothing, and `assess` appears only as a
directory path. `spec-lens`, asked to decide whether the concession was honest
or restatement with better manners, called it restatement — the criteria say
review judges the synthesis, review judges by the criteria, and the loop closes
on itself with nobody reading the document.

## What the fixes got right

Three of `spec-lens`'s findings were answered by arguments stronger than the
fixes it had proposed, and it said so. `codebase-lens` scored the trailer-keying
change against the bar it had written — its own standing-aside finding — and
reported that what arrived checks *state* where it had asked for *event*: weaker
against the exact failure it named, stronger against everything else, and better
overall. It held the finding rather than releasing it on that basis.

## Round two's outcome

**Another round owed.** Blocking: the three unsatisfiable criteria, the
regression, and the judge that does not judge.

# Round three — `9188ca6`

`codebase-lens` recommends **accept**, conditional on two one-line corrections.
`spec-lens` recommends **modify**, narrowly, with three small fixes and one
scope judgment referred to the repository's owner. Every round-two finding was
released.

## A retraction

`spec-lens` opened by withdrawing its round-one finding [10]:

> My finding [10] was wrong, and it damaged the document... My round-one grep
> was case-sensitive and missed it; round two I released [10] as "removed with
> the reasoning", which endorsed the regression rather than catching it.

The only regression across three rounds originated in a bad finding, was
released by its own raiser, and was caught in round three only because the brief
required re-running what proved each release. That is the case for the rule, and
it is recorded as evidence for it rather than as a reproach.

## Two findings the author would not have reached

**The `expected` route fails open.** `spec-lens` tested both directions. It
declared a substring with no check behind it: the runner reported "that check
has gone quiet" and the criterion went red, so the conjunction does real work.
It then disabled the matcher — `done < xt/fixture/expected` → `done < /dev/null`
— and the criterion went green with nothing behind it. All three fixture
criteria therefore rest on the one site `xt/fixture/expected` lists as uncovered
in its own ledger: "the self-test itself — nothing guards the guard."

**The recount was off by the paragraph that introduced it.** `codebase-lens`
found that the undetermined-population report is a fourth emission site with no
fixture case, so the arithmetic is 12 of 31 rather than 12 of 30, and the site
belongs in the `Uncovered:` list rather than silently changing the denominator.

## The document as a document

Both participants reached this independently, and it is the finding neither
lens produces on its own — it was asked for.

`codebase-lens`:

> The last third has drifted from deciding into defending, and four paragraphs
> are legible only to someone who read the review... The remedy is the
> decision's own thesis applied to itself. `docs/assessments/0004.md` exists...
> Provenance arguments, superseded criterion wordings and rebuttals of
> round-one numbers belong there; the decision keeps the conclusion. That is the
> live/archive split 0004 is *about*, and the decision is currently the one
> document in the pair not observing it.

`spec-lens` reached the same place: "the document has become a decision plus its
own review minute."

The four paragraphs, and what they were: a table insisting its own header was
true; a headline rebutting a number no reader had seen ("Seven sites, not two");
the review narrating its own reintroduction of a defect ("reintroduced by the
fix for it"); and a changelog of the decision's own revisions inside the
decision ("`'cited nowhere'` became `'cited nowhere in the synthesis'` between
revisions of this decision"). **This file is where they went.** The decision
keeps the conclusions.

`codebase-lens` named why the author would not have seen it:

> every one of those paragraphs was written to satisfy a participant, and three
> of the four were written to satisfy me.

It **stands aside** on the concern: a defect in the document as a document
rather than in the decision, nothing mechanical detects it, and not worth
holding acceptance for.

## Scope, reversed and then settled

`spec-lens` reversed its own round-one scope answer. Having said "one milestone,
not two", it argued in round three that the review lens step now delivers value
alone — for the three live documents that exist today, with no synthesis — and
that the decision's best argument for the step ("the first live document whose
correctness someone noticed nothing was checking") is an argument that the
change is larger than this decision. Entailment justifies sequencing, not
bundling.

Referred to the repository's owner, who kept the step in 0004. `spec-lens`
**stands aside**, having said it would.

It also named a risk that survives either answer: all three live documents
declare `covers: skills/`, so the lens fires on nearly every branch, and a
trigger that fires on everything is ignored the way one that fires on nothing
is. The decision now defines the quiet case as the common case rather than
leaving it to whoever types first.

## On the participants

Twice, unprompted, `codebase-lens` declined to let its own authorship pass
unremarked — once on releasing a finding against a bar it wrote, and once on
scoring a paragraph that restated its own reasoning:

> I note for the record that this is the second time I am scoring a paragraph
> written in answer to me, and that I released it on its argument rather than
> on its agreeing with me; the lead should weigh that accordingly.

`spec-lens` disclosed its own bias at the head of each of its last two reports
and retracted a finding that had cost the document a regression. Neither was
asked to do either thing. It is recorded as a fact about this session because
discernment's account of a resumed participant predicts the opposite, and the
postmortem should have the counter-evidence.

## Round three's outcome

**One accept with corrections, one modify.** All corrections from both were
applied in `5229471`. Not a fixed point: a round that raises nothing new has not
yet run.
