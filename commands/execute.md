---
description: "Drive the full SDLC execution loop — pick issues from DAG, TDD with review gates, close issues, complete milestone. Runs through by default; --step confirms each issue"
---

Use the crochet:execute skill to handle this request.


ARGUMENTS: $ARGUMENTS
